namespace NoFallDamage
{
    /// <summary>
    /// dummy interface to let the program to scan the whole project searching for
    /// classes to patch
    /// </summary>
    internal interface IPatchable
    {
    }
}
